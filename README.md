# Property-insurance-pricing
Frequency-severity pricing model for property insurance claims, built on real Wisconsin LGPIF data. Negative Binomial GLM for claim frequency, Gamma GLM for severity, combined into a pure premium (Gini 0.693). Includes a documented self-review that found and fixed two real statistical errors in the initial model.
